# Copy this to .env and fill in your keys — .env is gitignored.
# Format: OPENCOWORK_PROVIDER_<SLUG_UPPERCASE>_API_KEY=<key>
# where slug uses _ instead of -

OPENCOWORK_PROVIDER_OPENCODE_ZEN_API_KEY=
OPENCOWORK_PROVIDER_OPENAI_API_KEY=
